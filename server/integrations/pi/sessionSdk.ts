// The only backend gateway for Pi session persistence primitives.
// Feature modules import this Sylph-owned adapter instead of the package.
export { SessionManager } from "@earendil-works/pi-coding-agent";
export type { ModelRegistry } from "@earendil-works/pi-coding-agent";
